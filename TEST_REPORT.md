# 검증 결과

## 핵심 동작

| 검사 | 결과 |
|---|---|
| 초기 상태 | 중앙 분수공원 1개, 나머지 풀밭 |
| 도로 배치 | 고정 PNG가 아니라 Canvas 경로로 아스팔트·연석·차선 생성 |
| 도로 이웃 반응 | 끝점 자동 스냅 및 연결 구간 생성 확인 |
| 철길 배치 | 노반·침목·2개 레일을 동적으로 생성 |
| 건물 배치 | 부지와 가장 가까운 도로까지 진입로 생성 확인 |
| 이동·좌우 반전 | 지형 기하 전체 재계산 |
| 삭제 복구 | 추가 에셋 삭제 후 지형 캔버스가 초기 풀밭 상태와 동일한 해시로 복구 |

## 실제 Windows 브라우저 반응 테스트

| 상태 | Canvas 스크린샷 SHA-256 앞 12자리 | 관찰 |
|---|---|---|
| 분수공원만 존재 | `41b9d5c0cefe` | 기준 상태 |
| 사거리 추가 | `72a9b9df97d8` | 배경 도로 생성으로 변경 |
| 연결 도로 추가 | `554fdc637e8d` | 자동 연결 1개 생성 |
| 건물 추가 | `3ba04cc58a1c` | 부지와 진입로 1개 생성 |
| 추가 요소 모두 삭제 | `41b9d5c0cefe` | 기준 상태와 완전히 동일하게 복구 |

브라우저 콘솔의 페이지 오류는 0건이었습니다.

## 정적·알고리즘 검사

| 검사 | 결과 |
|---|---|
| `terrain-engine.js` 구문 | 통과 |
| `app.js` 구문 | 통과 |
| 동적 지형 Node 테스트 | 도로 12개 경로, 철길 5개 경로, 자동 연결 7개 생성 |
| 건물 진입로 테스트 | 통과 |
| 도로 삭제 후 파생 도로 수 | 0, 복구 확인 |
| 네트워크 스냅 | `(250,262)` 후보가 `(258,260)`으로 스냅 |
| TypeScript 지형 엔진 | strict + `noUncheckedIndexedAccess` 검사 통과 |
| 모바일 TS/TSX | 13개 파일 구문 파싱 통과 |
| 에셋 | 각 대상 폴더에 스프라이트 23개 + 오브젝트 23개 |
| 구형 지형 패치 | 폴더·코드 제거 확인 |
| JSON 설정 | 매니페스트, `package.json`, `app.json` 파싱 통과 |

## 실행 환경 제한

Windows 브라우저 버전은 실제 Chromium에서 인터랙션과 픽셀 복구까지 검증했습니다. 작업 환경의 npm 설치가 시간 제한 내 완료되지 않아 Expo Web/Android 번들 실행은 수행하지 못했습니다. 모바일 공용 지형 엔진은 strict TypeScript 검사했고, 전체 TS/TSX 소스는 구문 검사를 통과했습니다.
